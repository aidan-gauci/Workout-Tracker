import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.aidan.workouttracker',
  appName: 'Workout Tracker',
  webDir: 'dist'
};

export default config;
